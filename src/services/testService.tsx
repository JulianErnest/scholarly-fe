import {
  LoginFields,
  RegisterFields,
  TestFields,
  QuestionFields,
} from "../types/fields";
import api, { DefaultResponse } from "./api";

async function createTest() {
  try {
  } catch (error) {}
}
